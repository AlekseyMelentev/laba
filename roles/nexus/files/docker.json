{
    "name": "docker",
    "type": "groovy",
    "content": "security.setUserRoles('admin', ['nx-admin']); if (!repository.getRepositoryManager().exists('dockerhost')) { repository.createDockerHosted('dockerhost', 8283, null); }"
}
